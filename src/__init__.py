"""IntelliDocs AI — core RAG pipeline modules.

A modular Retrieval-Augmented Generation (RAG) pipeline:
PDF upload → text extraction → chunking → embeddings → FAISS vector store
→ retriever → LLM → grounded answer with source citations.
"""

__version__ = "0.1.0"
